# 🚧 STEP 2.0 — Implementation Boundary Specification

**Проект:** MontrixBot  
**Версия:** v2.0-preA (post-GD)  
**Статус:** Boundary fixed, implementation not started  
**Основание:**  
- SNAPSHOT.md  
- GD_STEP_2_X_AUTO_REAL.md  
- STEP_2_0_ARCHITECTURE.md  

---

## 1. Purpose

Этот документ:

- фиксирует **границы допустимой реализации STEP 2.0**,
- предотвращает неявное вмешательство в STEP 1.x,
- обязателен **перед началом любой реализации**.

Нарушение этого документа считается **нарушением канона проекта**.

---

## 2. Immutable Zone (STEP 1.x — READ ONLY)

Следующие компоненты **запрещено модифицировать**:

- торговая логика стратегий,
- алгоритмы входа / выхода,
- риск-менеджмент,
- исполнение ордеров,
- SIM-аналитика и ScoutNotes,
- существующие UI control-surface 1.x,
- PolicyTrace (как observability).

Допускается **только чтение** и подписка на события.

---

## 3. Allowed Integration Points

STEP 2.0 может интегрироваться **только** через следующие точки:

### 3.1. Before Open Position

Единственная допустимая точка контроля:

before_open_position(symbol, strategy_id)

yaml
Копировать код

STEP 2.0 может:

- разрешить или запретить открытие,
- вернуть причины запрета,
- зафиксировать Gate decision.

STEP 2.0 **не может**:

- менять параметры сделки,
- менять стратегию,
- инициировать сделку самостоятельно.

---

### 3.2. Trading Cycle Control

STEP 2.0 может:

- остановить торговый цикл,
- разрешить возобновление цикла.

STEP 2.0 **не может**:

- форсировать открытие сделок,
- вмешиваться в управление открытыми позициями.

---

## 4. Supervisor Authority Limits

Supervisor (STEP 2.0):

- **не владеет** стратегиями,
- **не владеет** ордерами,
- **не владеет** позициями,
- **не владеет** рынком.

Supervisor владеет **только**:

- разрешением / запретом,
- режимом автономии,
- состоянием паузы,
- коммуникацией причин человеку.

---

## 5. Forbidden Actions (Hard Ban)

STEP 2.0 **ЗАПРЕЩЕНО**:

- автоматически запускать торговлю после `HARD_STOP`,
- инициировать сделки без стратегии,
- изменять поведение стратегии,
- подменять SIM-сигналы,
- делать Gate = ALLOW без логируемых причин,
- принимать “тихие” решения без explainability,
- расширять UI control-surface без governance.

---

## 6. Mandatory Explainability

Каждое из событий обязано иметь причины:

- Gate VETO,
- Auto Pause,
- Resume,
- Tech Block,
- Strategy Lock refusal.

Формат:
- 2–3 причины,
- человекочитаемо,
- без внутренней реализации.

---

## 7. Human Authority Rule

- Человек — **абсолютная инстанция**.
- Ручной STOP:
  - необратим для автоматики,
  - требует явного Resume.
- STEP 2.0 не имеет права “догадываться” за человека.

---

## 8. Audit Rule

Перед первым запуском реализации STEP 2.0 должно быть подтверждено:

- архитектура зафиксирована,
- границы реализации зафиксированы,
- список модулей утверждён,
- rollout-план отсутствует (реализация поэтапная).

---

### ✔ STEP 2.0 Implementation Boundary Fixed
