# MontrixBot Master Rules v1.0

<a name="0-rule-hierarchy"></a>
## 0. Иерархия Правил (Rule Hierarchy)

Для обеспечения внутренней согласованности документа и отсутствия противоречий устанавливается следующая приоритетность правил:

<a name="0-1-1-hard"></a>
### 0.1. Уровень 1 — Архитектурные правила (HARD)
Наивысший приоритет.  
Состоят из:
- Core Engineering & Code Rules
- Addendum (обязательные архитектурные расширения)

Эти правила определяют устройство системы, слойность, ограничения, запреты, изоляцию, чистоту архитектуры.  
Они **не могут быть отменены**, изменены или ослаблены никакими другими разделами документа.

---

<a name="0-2-2-process"></a>
### 0.2. Уровень 2 — Процессные стандарты (PROCESS)
Состоят из:
- Development Standard (v2.0)

Этот уровень определяет:
- workflow разработки  
- качество  
- UX / интеграции  
- lifecycle  
- правила сопровождения  

---

<a name="0-3-3-extended-process"></a>
### 0.3. Уровень 3 — Расширенные процессные правила (EXTENDED PROCESS)
Состоят из:
- Extended Development Rules (v1.1)

Этот уровень включает:
- baseline naming  
- проектный аудит  
- partial ZIP  
- project_meta.json / manifest.txt  
- README_AUDIT.md  
- Patch File Request Autonomy  
- Audit Automation Rules  

---

<a name="0-4-4"></a>
### 0.4. Уровень 4 — Специальные протоколы
Например:
- Anti-Overheat Protocol

Эти правила добавляют оптимизации стабильности, но не меняют архитектуру.

---

<a name="0-5"></a>
### 0.5. Разрешение конфликтов
1. Побеждает более высокий уровень.  
2. Extended Rules могут обновлять процессы, но не архитектуру.  
3. Устаревшие примеры не считаются конфликтами.  
4. В случае неоднозначности решение принимает ассистент по приоритету уровней.

---

<a name="0-6"></a>
### 0.6. Инвариантность документа
Все правила объединены в единую нормативную систему.

Полный консолидированный норматив проекта.

--------------------------------------------

<a name="1-core-engineering-code-rules"></a>

## 0.7. Glossary of System Layers

Для исключения неоднозначности, слои системы определяются так:

- **UI** — отображение, события, пользовательские взаимодействия.
- **UIAPI** — адаптация данных от ядра к UI, выдача snapshot.
- **StateEngine** — единый источник истины, изменение состояния.
- **core** — расчёты, логика, торговые модули, TPSL.
- **runtime** — файлы состояния, persistent storage.
- **sim-engine** — симулятор торгового поведения.
- **executors** — запуск действий (ордеры, TPSL, обновления).

## 1. Core Engineering & Code Rules
# MontrixBot Code Rules — Version 1.2 (Release Edition)

*Complete Engineering & Architecture Specification*

---

# Оглавление

1. Философия и принципы разработки
2. Архитектура системы
3. Правила работы со слоями
4. Состояние, данные и сериализация
5. Ошибки, исключения и логирование
6. Структура файлов и модулей
7. Процесс разработки и патч-политика
8. Правила UI слоя
9. Дополнительные принципы (HARD / SOFT / LEGACY)
10. Анти-правила
11. TL;DR — супер-краткое резюме
12. Примеры хорошего и плохого кода
13. Приложение: Конвенции именования

---

# 1. Философия разработки (уровень 0)

<a name="1-1"></a>
## 1.1 Главный порядок приоритетов

**Безопасно → Стабильно → Предсказуемо → Расширяемо → Поддерживаемо → Красиво**

<a name="1-2"></a>
## 1.2 Инженерный порядок качества

**Надёжно → Стабильно → Модульно → Безопасно → Быстро → Красиво**

<a name="1-3"></a>
## 1.3 Девиз разработки

**Надёжно → Стабильно → Быстро → Красиво**

---

# 2. Архитектура системы (HARD)

<a name="2-1"></a>

### 1.99. Связь с Development Standard
Все правила Development Standard расширяют, но не изменяют архитектурные запреты Code Rules.

### HARD Rule: UIAPI Must Remain Pure
UIAPI не должен содержать:
- бизнес-логику,
- состояние,
- обработку ошибок ядра,
- orchestration-потоков,
- преобразование snapshot → новый state.

UIAPI выполняет только адаптацию данных и передачу команд.

## 2.1 Жёсткая цепочка зависимостей

```
UI → UIAPI → StateEngine → core
```

Запрещено любое движение "вверх".

<a name="2-2"></a>

### 1.99. Связь с Development Standard
Все правила Development Standard расширяют, но не изменяют архитектурные запреты Code Rules.

### HARD Rule: UIAPI Must Remain Pure
UIAPI не должен содержать:
- бизнес-логику,
- состояние,
- обработку ошибок ядра,
- orchestration-потоков,
- преобразование snapshot → новый state.

UIAPI выполняет только адаптацию данных и передачу команд.

## 2.2 Роли слоёв

| Слой        | Функция               | Запрещено                              |
| ----------- | --------------------- | -------------------------------------- |
| UI          | отображение           | логика, изменение state, чтение файлов |
| UIAPI       | подготовка данных UI  | менять state                           |
| StateEngine | управление состоянием | зависеть от UI                         |
| core        | расчёты               | импортировать UI/UIAPI                 |

---

# 3. Правила работы со слоями (HARD)

* Pure Modules
* Thin Layers
* File-as-API
* Explicit Dependency Rule
* Continuous Context
* Stable Integration Points

---

# 4. Состояние, данные и сериализация

<a name="4-1-stateengine"></a>
## 4.1 StateEngine — единственный источник истины

Меняет позиции, equity, TPSL, режимы.

<a name="4-2-diff"></a>
## 4.2 DIFF ключевого состояния

Фиксация изменений: позиции, equity, режимы.

<a name="4-3-stable-data-shapes"></a>
## 4.3 Stable Data Shapes

Фиксированные структуры snapshot.

<a name="4-4-zero-hidden-i-o"></a>
## 4.4 Zero Hidden I/O

Файловые операции — только в runtime-слое.

<a name="4-5-testability-by-design"></a>
## 4.5 Testability by Design

Лёгкость тестирования каждого модуля.

---

# 5. Ошибки, исключения и логирование

<a name="5-1-thin-exceptions-boundary-hard"></a>
## 5.1 Thin Exceptions Boundary (HARD)

Пробрасывание ошибок до границы слоя.

<a name="5-2-silent-fail-hard"></a>
## 5.2 Запрет silent fail (HARD)

Никаких `except: pass`.

<a name="5-3-fail-fast-log-hard"></a>
## 5.3 Fail Fast & Log Hard

Падение при ошибке + логирование контекста.

<a name="5-4-hard"></a>
## 5.4 Говорящие ошибки (HARD)

Логи должны показывать причину, параметры, контекст.

---

# 6. Структура файлов и модулей

<a name="6-1-strict-file-structure"></a>
## 6.1 Strict File Structure

Импорты → Константы → Типы → Классы → Функции → Точка входа.

<a name="6-2-no-teleporting-logic"></a>
## 6.2 No Teleporting Logic

Логика одной темы в одном месте.

<a name="6-3-no-magic-imports-hard"></a>
## 6.3 No Magic Imports (HARD)

Импорт не должен выполнять логику.

---

# 7. Процесс разработки и патч-политика

<a name="7-1-baseline-hard"></a>
## 7.1 Патч только после baseline (HARD)

<a name="7-2-hard"></a>
## 7.2 Перед патчем (HARD)

* Проверить baseline
* Поднять память
* Сверить правила
* Проверить структуру

<a name="7-3"></a>
## 7.3 После патча

* Присвоить новый STEP
* Создать новый baseline

<a name="7-4-soft"></a>
## 7.4 Один патч — одна задача (SOFT)

Допустима чистка в рамках изменения.

---

# 8. Правила UI слоя

UI запрещено:

* выполнять логику
* менять state
* читать runtime-файлы
* импортировать StateEngine/core напрямую

UI получает только snapshot от UIAPI.

---

# 9. Additional Principles (HARD / SOFT / LEGACY)

<a name="hard"></a>
## 🟥 HARD

* Single Source of Truth for Config
* Deterministic Execution
* Time Boundaries Rule
* Zero Hidden I/O
* Compatibility Layer
* Stable Integration Points

<a name="soft"></a>
## 🟨 SOFT

* Document-as-you-code
* Fail Fast & Log Hard
* Strict File Structure

<a name="legacy"></a>
## 🟦 LEGACY

* Пока пустой блок

---

# 10. Анти-правила (запреты)

❌ Логика в UI
❌ Silent fail
❌ Глобальные изменяемые структуры
❌ Teleporting logic
❌ Magic imports
❌ Изменение state вне StateEngine
❌ Несвязанные задачи в одном патче
❌ Нестабильные snapshot-форматы
❌ Сериализация внутри core
❌ Обход UIAPI
❌ Обратные зависимости
❌ Патч без baseline

---

# 11. TL;DR

✔ Чистые слои
✔ Явные зависимости
✔ Стабильные структуры данных
✔ Контракты перед реализацией
✔ Говорящие ошибки
✔ Маленькие патчи

❌ Логика в UI
❌ Silent fail
❌ Hidden I/O
❌ Magic imports
❌ Большие комбайны
❌ Глобальные переменные

---

# 12. Примеры

<a name=""></a>
## Плохо

```python
def on_click(): state['p'].append(1)
```

<a name=""></a>
## Хорошо

```python
def on_click(): engine.send_action(ActionRequest(...))
```

---

# 13. Конвенции именования

Рекомендуемые суффиксы:

* *_snapshot
* *_state
* *_request
* *_result
* *_event

Файлы и модули должны быть короткими, точными и отражать ответственность.

---

# 14. Advanced Engineering Principles (v1.2)

<a name="hard"></a>
## 🟥 HARD

<a name="14-1-stable-integration-points"></a>
### 14.1 Stable Integration Points (расширенный)

Каждая точка интеграции должна иметь фиксированный формат данных, защиту от некорректного ввода, документированное поведение и минимальные изменения между версиями.

<a name="14-2-graceful-degradation"></a>
### 14.2 Graceful Degradation

При сбоях отдельных компонентов система должна продолжать работать: REST вместо WebSocket, last-known-good snapshot, автоматическое создание недостающих runtime-файлов.

<a name="14-3-non-blocking-architecture"></a>
### 14.3 Non-blocking Architecture

UI, StateEngine и Executor не должны блокировать выполнение друг друга. Тяжёлые операции переносятся в отдельные слои.

<a name="14-4-safe-defaults-everywhere"></a>
### 14.4 Safe Defaults Everywhere

При отсутствии данных или ошибках конфигурации выбирается безопасное поведение.

<a name="14-5-no-cross-layer-knowledge"></a>
### 14.5 No Cross-Layer Knowledge

Слой не должен знать внутренние структуры других слоёв.

<a name="14-6-minimal-state-surface"></a>
### 14.6 Minimal State Surface

StateEngine раскрывает только минимально необходимую информацию наружу.

<a name="14-7-predictable-cycle-timing"></a>
### 14.7 Predictable Cycle Timing

Все интервалы обновлений циклов должны быть фиксированы и документированы.

<a name="14-8-centralized-logging-pipeline"></a>
### 14.8 Centralized Logging Pipeline

Все компоненты используют единый формат логирования.

<a name="14-9-custom-error-types"></a>
### 14.9 Custom Error Types

Для критичных ошибок используются специальные классы исключений.

<a name="soft"></a>
## 🟨 SOFT

<a name="14-10-minimum-surprise-pola"></a>
### 14.10 Minimum Surprise (POLA)

Код и API должны вести себя так, как ожидает разработчик: без скрытых побочных эффектов.

---

Рекомендуемые суффиксы:

* *_snapshot
* *_state
* *_request
* *_result
* *_event

Файлы и модули должны быть короткими, точными и отражать ответственность.


--------------------------------------------

<a name="2-development-standard-v2-0"></a>

### 1.99. Связь с Development Standard
Все правила Development Standard расширяют, но не изменяют архитектурные запреты Code Rules.

### HARD Rule: UIAPI Must Remain Pure
UIAPI не должен содержать:
- бизнес-логику,
- состояние,
- обработку ошибок ядра,
- orchestration-потоков,
- преобразование snapshot → новый state.

UIAPI выполняет только адаптацию данных и передачу команд.

## 2. Development Standard (v2.0)
# MontrixBot_Development_Standard_v2.0

Полный корпоративный стандарт разработки, процессов, UX, стратегии, безопасности и эксплуатационной дисциплины MontrixBot.
Версия 2.0 — максимальное, конечное, расширенное издание, включающее все правила, методологии, pipeline, контроль качества, SLA, RACI, Red Zones, UX, архитектурные нормы и эксплуатационные регламенты.

---

# 📘 Оглавление

1. Введение
2. Цели стандарта
3. Область применения
4. Принципы дизайна (Design Philosophy)
5. Глоссарий (устойчивые термины)
6. MUST / SHOULD / MAY — уровни обязательности
7. Workflow Pipeline (полный жизненный цикл)
8. Quality Gates (контрольные точки качества)
9. Red Zone — абсолютные запреты
10. RACI Matrix — роли и ответственность
11. Development Standards — стандарты разработки

    * 11.1 Baseline Protocol
    * 11.2 Patch Protocol
    * 11.3 Audit Protocol
    * 11.4 QA & Smoke Tests
    * 11.5 Change Safety Protocol
12. Communication Standards — правила взаимодействия
13. UX/UI Standards — стандарты интерфейса

    * 13.1 Core UX
    * 13.2 Navigation
    * 13.3 Performance
    * 13.4 UX Safety Rules
    * 13.5 UI Error Interaction
14. Trading Behavior Standards — торговые стандарты
15. Integration Standards — интеграции
16. Versioning & Roadmap Standards
17. Telegram Bot Standards
18. Runtime Standards
19. Archive Handling Standards
20. Interaction Rules: UIAPI ↔ StateEngine
21. Security Standards
22. AI Module Standards
23. Strategic UI/UX Standards
24. Product SLA (уровни гарантированного качества)
25. Extended Operational Standards
26. Full Non-Code Rule Reference

---

# 1. Введение

MontrixBot Development Standard v2.0 — окончательный норматив разработки, процессов, UX, стандартов безопасности и механики эксплуатации проекта.

---

# 2. Цели стандарта

* Зафиксировать непротиворечивую методологию разработки.
* Создать единый корпоративный стандарт.
* Гарантировать предсказуемость поведения системы.
* Уменьшить технические риски.
* Обеспечить масштабируемость и расширяемость.

---

# 3. Область применения

Стандарт обязателен:

* при создании патчей
* при ревью архива
* при изменении логики
* при создании новых модулей
* при релизах и обновлениях
* при планировании и roadmap

---

# 4. Принципы дизайна (Design Philosophy)

* Стабильность системы выше скорости разработки.
* Предсказуемость поведения выше экспериментальности.
* Архитектурная чистота выше кратковременной выгоды.
* Ошибки должны быть видимыми, а не скрытыми.
* Изменения должны быть обратимыми.
* Чем проще правило — тем надёжнее система.

---

# 5. Глоссарий (устойчивые термины)

* **Baseline** — эталонный архив проекта.
* **Patch** — одно изменение.
* **STEP** — конкретная версия патча.
* **Snapshot** — стабильная форма состояния.
* **Runtime** — рабочая среда.
* **Strategy** — торговая логика.
* **Anchor Coin** — стабильный актив.
* **Rocket Coin** — высокорисковый актив.
* **Pipeline** — жизненный цикл разработки.

---

# 6. Уровни обязательности (MUST / SHOULD / MAY)

* **MUST** — обязательное.
* **SHOULD** — желательно.
* **MAY** — опционально.

---

# 7. Workflow Pipeline (жизненный цикл)

```
REQUEST → ANALYZE → PATCH → VERIFY → RELEASE → BASELINE
```

Описание этапов:

* **REQUEST** — запрос на изменения.
* **ANALYZE** — анализ архива, структуры, стандартов.
* **PATCH** — создание минимального изменения.
* **VERIFY** — QA + Gates.
* **RELEASE** — присвоение STEP.
* **BASELINE** — создание нового архива.

---

# 8. Quality Gates

1. **Architecture Gate** — соответствие Code Rules.
2. **Structure Gate** — структура проекта.
3. **Behavior Gate** — корректность поведения.
4. **Safety Gate** — ключи, режимы, runtime.
5. **Release Gate** — версия, baseline.

---

# 9. Red Zone — абсолютные запреты (MUST NOT)

* Патч без baseline.
* Silent fail.
* Изменение state вне StateEngine.
* Обход UIAPI.
* Изменение runtime без протокола.
* Добавление ключей в код.
* Произвольные файлы в корне.

---

# 10. RACI Matrix

| Процесс             | ChatGPT | User |
| ------------------- | ------- | ---- |
| Стратегия           | C       | A    |
| Архитектура         | A       | C    |
| Патч                | A       | R    |
| Продуктовые решения | C       | A    |
| Архив               | A       | C    |

---

# 11. Development Standards

<a name="11-1-baseline-protocol"></a>
## 11.1 Baseline Protocol

* Работа начинается после получения архива (MUST).
* Проверка структуры (MUST).

<a name="11-2-patch-protocol"></a>
## 11.2 Patch Protocol

* Один патч — одна задача.
* Назначение STEP.
* Новый baseline.

<a name="11-3-audit-protocol"></a>
## 11.3 Audit Protocol

* Поднять память.
* Поднять Code Rules.
* Поднять Development Standard.
* Проверить структуру.
* Проверить runtime.

<a name="11-4-qa-smoke-tests"></a>
## 11.4 QA & Smoke Tests

* UI загружается.
* StateEngine работает.
* runtime читается.
* симулятор отвечает.

<a name="11-5-change-safety-protocol"></a>
## 11.5 Change Safety Protocol

* любые изменения должны быть обратимы.

---

# 12. Communication Standards

* Пользователь управляет продуктом.
* ИИ реализует архитектуру.
* Новый чат = продолжение baseline.

---

# 13. UX/UI Standards

<a name="13-1-core-ux"></a>
## 13.1 Core UX

* Стиль MoonBot.
* Темная тема.

<a name="13-2-navigation"></a>
## 13.2 Navigation

* Панель режимов сверху.
* График — центр.
* Таблицы — справа.

<a name="13-3-performance"></a>
## 13.3 Performance

* Обновление UI 100–200 мс.
* UI не блокирует StateEngine.

<a name="13-4-ux-safety"></a>
## 13.4 UX Safety

* UI всегда имеет fallback.

<a name="13-5-ui-error-interaction"></a>
## 13.5 UI Error Interaction

* Ошибки должны быть понятны.

---

# 14. Trading Behavior Standards

* 2 якоря + 1 ракета.
* Внутренний трейлинг.
* Просадка -5%.
* SIM по умолчанию.

---

# 15. Integration Standards

* Binance Docs — истина.
* Контроль rate limits.
* Retry-политика.

---

# 16. Versioning & Roadmap

* Рабочий + идеальный roadmap.
* После 1.0 → 1.1.1 → 1.1.

<a name="16-1-roadmap-governance"></a>
## 16.1 Roadmap Governance (Управление дорожной картой)

<a name="1-roadmap-must"></a>
### 1. Следование рабочему roadmap — MUST

Все патчи, версии и архитектурные изменения обязаны соответствовать рабочему roadmap. Он является источником истины в вопросах текущих приоритетов.

<a name="2-roadmap-should"></a>
#
### 1.99. Связь с Development Standard
Все правила Development Standard расширяют, но не изменяют архитектурные запреты Code Rules.

### HARD Rule: UIAPI Must Remain Pure
UIAPI не должен содержать:
- бизнес-логику,
- состояние,
- обработку ошибок ядра,
- orchestration-потоков,
- преобразование snapshot → новый state.

UIAPI выполняет только адаптацию данных и передачу команд.

## 2. Использование идеального roadmap — SHOULD

Идеальный roadmap применяется когда позволяет контекст, когда нет рисков для стабильности, и когда улучшения могут быть выполнены без нарушения текущих задач.

<a name="3-roadmap"></a>
### 3. Несоответствие рабочей roadmap → патч запрещён

Если предлагаемое изменение противоречит рабочему roadmap, оно переносится в backlog или в идеальный roadmap для реализации в будущем.

<a name="4-roadmap"></a>
### 4. Обновление roadmap — только пользователем

Пользователь является владелцем продукта и определяет направление развития. ChatGPT следует roadmap и реализует архитектуру в соответствии с ним.

<a name="5-roadmap"></a>
### 5. Идеальный roadmap влияет на архитектурный дизайн

Он используется как источник будущих требований и определяет долгосрочную расширяемость и структуру системы.

<a name="6-roadmap"></a>
### 6. Рабочий roadmap влияет на текущую реализацию

Он определяет порядок выполнения задач, содержание патчей и логику движения по STEP.

<a name="7-roadmap-baseline-protocol"></a>
### 7. Roadmap — часть Baseline Protocol

Перед каждым патчем ChatGPT обязан проверить соответствие roadmap: не нарушает ли изменение текущий этап, соответствует ли оно плану и не требует ли переноса.

* Рабочий + идеальный roadmap.
* После 1.0 → 1.1.1 → 1.1.

---

# 17. Telegram Bot Standards

* /позиции /закрыть /tp /sl.
* 2FA на критичные команы.

---

# 18. Runtime Standards

* переносимость.
* полная сохранность state.

---

# 19. Archive Handling Standards

* всегда полный архив.
* полная проверка.
* сообщения о несоответствиях.

---

# 20. UIAPI ↔ StateEngine Interaction

* UIAPI = внешний snapshot.
* StateEngine = внутренний snapshot.
* UI = отображение состояния.

---

# 21. Security Standards

* ключи только в .env.
* REAL вручную.
* SIM при тестах.

---

# 22. AI Module Standards

* экспериментальная вкладка.
* ручная проверка.
* сбор данных.

---

# 23. Strategic UI/UX Standards

* TP/SL на графике.
* Equity-бар.
* Heatmap.

---

# 24. Product SLA

* UI ≤ 200 мс.
* действия ≤ 100 мс.
* запуск ≤ 1 сек.
* загрузка runtime ≤ 200 мс.

---

# 25. Extended Operational Standards

* система должна быть самовосстанавливающейся.
* ошибки должны быть видимы.
* логирование — полное.

---

# 26. Full Non-Code Rule Reference

(Актуализировано, включает все правила проекта в едином виде.)


--------------------------------------------

<a name="3-addendum-v1-0"></a>
## 3. Addendum (v1.0)
# MontrixBot Development Addendum v1.0
<a name="montrixbot-development-standard-v2-0"></a>
### Дополнение к MontrixBot Development Standard v2.0  
<a name="active"></a>
### Статус: ACTIVE  
<a name="step-1-2-10-pre2-baseline"></a>
### Актуально для всех STEP, начиная с 1.2.x

---

<a name="1-ui-update-protocol"></a>
## 1. UI Update Protocol (обязательный)

Все обновления интерфейса должны выполняться **только** через единый
`ui_updater` / event loop.

<a name=""></a>
### Требования:
- Никаких прямых обновлений UI из TPSL loop, Executor, streams или других потоков.
- Все сигналы на обновление проходят через единый диспетчер событий.
- Изменения UI происходят только в главном UI-потоке.

<a name=""></a>
### Цель:
Предотвратить гонки потоков, рассинхронизацию, двойные обновления, подвисания UI и состояние “UI not responding”.

---

<a name="2-state-consistency-check-layer"></a>

### 1.99. Связь с Development Standard
Все правила Development Standard расширяют, но не изменяют архитектурные запреты Code Rules.

### HARD Rule: UIAPI Must Remain Pure
UIAPI не должен содержать:
- бизнес-логику,
- состояние,
- обработку ошибок ядра,
- orchestration-потоков,
- преобразование snapshot → новый state.

UIAPI выполняет только адаптацию данных и передачу команд.

## 2. State Consistency Check Layer

Необходим специальный слой проверки согласованности состояния между:

- runtime state  
- UI state  
- SIM engine  
- tradebook  
- open positions  
- TPSL loop  

<a name=""></a>
### Требования:
- Создать sanity-модуль.
- Выполнять проверку периодически или при ключевых изменениях.

<a name=""></a>
### Цель:
Предотвратить критические несоответствия состояния, когда UI, TPSL и runtime представляют разные версии реальности.

---

<a name="3-ui-core-isolation-rule"></a>
## 3. UI–Core Isolation Rule

Интерфейс пользователя **не имеет права** вызывать ядро напрямую.

<a name=""></a>
### Разрешено:
`UI → ui_api → core`

<a name=""></a>
### Запрещено:
`UI → core.function()`

<a name=""></a>
### Причины:
- предотвращение дедлоков,
- единый управляемый канал бизнес-логики,
- корректная работа StateBinder и Executor,
- стабильность многопоточного окружения.

---

<a name="4-heartbeat-watchdog-requirement"></a>
## 4. Heartbeat / Watchdog Requirement

Следующие подсистемы обязаны иметь heartbeat/watchdog:

- WebSocket feed  
- TPSL loop  
- Executor  

<a name=""></a>
### Цели:
- обнаружение зависаний,
- автоматическое восстановление потоков,
- защита от silent-fail сценариев.

---

<a name="5-project-snapshot-procedure"></a>
## 5. Project Snapshot Procedure

При завершении работы в чате выполняется команда:

**“Сделай снимок проекта.”**

Snapshot должен содержать:

- текущий STEP,
- baseline,
- Roadmap A/B,
- Extended Roadmap v2.0 (актуальные статусы),
- структурные правила,
- ограничения,
- архитектурные решения,
- активные изменения,
- статус UI (включая перенос app_step9),
- важные технические выводы.

В новом чате Snapshot загружается командой:

**“Загружаю проект MontrixBot. Используй этот снимок как память.”**

<a name=""></a>
### Цель:
Поддерживать непрерывность разработки без потерь контекста.

---

<a name="6-ui-runtime-isolation"></a>
## 6. UI Runtime Isolation

UI запрещено читать файлы из `/runtime` напрямую:

- `runtime/state.json`
- `runtime/sim_state.json`
- `runtime/tpsl_config.json`
- любые JSON/STATE-файлы runtime

<a name=""></a>
### Разрешено:
`core → ui_api → UI`

<a name=""></a>
### Причины:
- контроль целостности состояния,
- предотвращение гонок при записи,
- безопасная работа TPSL loop и StateBinder.

---

<a name="7-tpsl-loop-independence"></a>
## 7. TPSL Loop Independence

TPSL loop работает **полностью автономно**, без зависимости от UI.

<a name=""></a>
### Требования:
- TPSL не должен ждать UI.
- TPSL не должен обращаться к UI.
- UI не должен влиять на работу цикла SL/TP.

<a name=""></a>
### Цель:
Гарантировать стабильную работу стопов и тейков при любых задержках или подвисаниях UI.

---

# Статус применения

- ✔ Правила немедленно активны.  
- ✔ Обязательны для всех патчей, изменений, новых модулей.  
- ✔ Интегрированы в Model Set Context проекта.  
- ✔ При генерации патчей автоматически сверяются с Code Rules v1.2, Development Standard v2.0 и данным Addendum.

---

# Конец документа


--------------------------------------------

<a name="4-extended-rules-v1-1"></a>
## 4. Extended Rules (v1.1)
# MontrixBot Development Extended Rules v1.1

<a name=""></a>
## Объединённые стандарты:

-   Baseline Naming
-   Patch Audit Protocol
-   Testing & Validation Checklist
-   Patch File Request Autonomy (new in v1.1)

Статус: **ACTIVE**\
Версия: **v1.1**\
Актуально: **STEP1.3.x**

------------------------------------------------------------------------

<a name="1-baseline-naming-rule-unified-versioning-standard"></a>
## 1. Baseline Naming Rule (Unified Versioning Standard)

Все baseline-файлы должны иметь формат:

MontrixBot_STEP`<step>`{=html}\_`<Tag>`{=html}\_pre`<N>`{=html}\_BASELINE

<a name=""></a>
### Основные правила:

-   `<step>` --- всегда текущий активный шаг Roadmap (например: 1.3.1,
    1.3.2, 1.3.3).\
-   При переходе на новый STEP счётчик `pre` сбрасывается на `pre1`.\
-   Если закрывается долг предыдущего шага, baseline всё равно
    использует **текущий STEP**,\
    а долг отражается только в `<Tag>`, например:\
    `Debt_1.3.1`, `Debt_1.2.7`.\
-   История версии формируется через `<Tag>` и `pre<N>`.\
-   Старые STEP не используются в именах baseline-файлов.

------------------------------------------------------------------------

<a name="2-patch-audit-protocol"></a>

### 1.99. Связь с Development Standard
Все правила Development Standard расширяют, но не изменяют архитектурные запреты Code Rules.

### HARD Rule: UIAPI Must Remain Pure
UIAPI не должен содержать:
- бизнес-логику,
- состояние,
- обработку ошибок ядра,
- orchestration-потоков,
- преобразование snapshot → новый state.

UIAPI выполняет только адаптацию данных и передачу команд.

## 2. Patch Audit Protocol (обязательный)

После каждого патча ассистент обязан автоматически:

<a name="2-1"></a>
#
### 1.99. Связь с Development Standard
Все правила Development Standard расширяют, но не изменяют архитектурные запреты Code Rules.

### HARD Rule: UIAPI Must Remain Pure
UIAPI не должен содержать:
- бизнес-логику,
- состояние,
- обработку ошибок ядра,
- orchestration-потоков,
- преобразование snapshot → новый state.

UIAPI выполняет только адаптацию данных и передачу команд.

## 2.1. Обновлять правила

-   Указывать, какие изменения необходимо внести в
    Standards/Addendum/Naming Rules.\
-   Предоставлять готовый текст, который пользователь вставляет в
    документацию.

<a name="2-2"></a>
#
### 1.99. Связь с Development Standard
Все правила Development Standard расширяют, но не изменяют архитектурные запреты Code Rules.

### HARD Rule: UIAPI Must Remain Pure
UIAPI не должен содержать:
- бизнес-логику,
- состояние,
- обработку ошибок ядра,
- orchestration-потоков,
- преобразование snapshot → новый state.

UIAPI выполняет только адаптацию данных и передачу команд.

## 2.2. Указывать, что пользователь должен прислать

-   Partial ZIP или полный ZIP.\
-   Список изменённых файлов.\
-   Какие модули по UI/Core/Runtime нужно показать.

<a name="2-3"></a>
#
### 1.99. Связь с Development Standard
Все правила Development Standard расширяют, но не изменяют архитектурные запреты Code Rules.

### HARD Rule: UIAPI Must Remain Pure
UIAPI не должен содержать:
- бизнес-логику,
- состояние,
- обработку ошибок ядра,
- orchestration-потоков,
- преобразование snapshot → новый state.

UIAPI выполняет только адаптацию данных и передачу команд.

## 2.3. Генерировать служебные файлы

Ассистент создаёт:

-   `project_meta.json`\
-   `manifest.txt` (хеши файлов)\
-   `README_AUDIT.md`

<a name="2-4-baseline"></a>
#
### 1.99. Связь с Development Standard
Все правила Development Standard расширяют, но не изменяют архитектурные запреты Code Rules.

### HARD Rule: UIAPI Must Remain Pure
UIAPI не должен содержать:
- бизнес-логику,
- состояние,
- обработку ошибок ядра,
- orchestration-потоков,
- преобразование snapshot → новый state.

UIAPI выполняет только адаптацию данных и передачу команд.

## 2.4. Обновлять baseline

Ассистент предоставляет:

-   точное имя нового baseline,\
-   корректный STEP, Tag и pre`<N>`{=html},\
-   статус Roadmap и следующее действие.

------------------------------------------------------------------------

<a name="3-testing-validation-checklist"></a>
## 3. Testing & Validation Checklist (обязательная процедура)

<a name="3-1"></a>
### 3.1. Инструкции по тестированию

Включают:

-   как запустить проект\
-   какие функции протестировать\
-   ожидания по поведению\
-   возможные симптомы ошибок\
-   инструкции по воспроизведению багов

<a name="3-2"></a>
### 3.2. Что пользователь должен прислать после тестирования

Ассистент всегда указывает, какие артефакты собрать:

-   `health_log.jsonl`\
-   `ui_log.txt`\
-   `tpsl_loop.log` (если есть)\
-   скриншоты графика\
-   скриншоты UI\
-   короткое видео при зависаниях\
-   состояния runtime:
    -   `runtime/state.json`
    -   `runtime/sim_state.json`

<a name="3-3"></a>
### 3.3. Чеклист правильной работы

□ ChartRouter получает snapshot стабильно □ TickUpdater не создаёт
дубликатов событий □ UIUpdateProtocol соблюдён □ UI не вызывает core
напрямую □ TPSL loop работает автономно □ Нет ошибок в консоли □ UI не
зависает при переключении монет □ StateBinder не выдаёт предупреждений

------------------------------------------------------------------------

<a name="4-patch-file-request-autonomy"></a>
## 4. Patch File Request Autonomy (обязательное правило)

Ассистент полностью отвечает за определение того, какие файлы необходимы
для патча.

<a name=""></a>
### Основные положения:

1.  Пользователь **не обязан** знать, какие файлы нужны.\

2.  После генерации плана патча ассистент обязан:

    -   указать точный список файлов (partial ZIP);\
    -   запросить дополнительные файлы при необходимости;\
    -   сгенерировать служебные файлы:
        -   `project_meta.json`,\
        -   `manifest.txt`,\
        -   `README_AUDIT.md`.

3.  Если в процессе анализа требуется файл, который давно не изменялся,\
    ассистент обязан запросить его отдельно.

4.  Пользователь отправляет только те файлы, которые запросил ассистент
    ---\
    не больше и не меньше.

5.  Ответственность за корректность состава патча полностью лежит на
    ассистенте.

------------------------------------------------------------------------
<a name="5-audit-automation-rules"></a>
## 5. Audit Automation Rules (новый обязательный раздел)

Для ускорения и стандартизации аудита ассистент обязан автоматически выполнять следующие действия после анализа каждого ZIP-архива или partial ZIP:

<a name="5-1-project-meta-json"></a>
### 5.1. Генерация project_meta.json
Ассистент создает полный и актуальный `project_meta.json`, содержащий:
- текущую baseline-версию,
- текущий STEP и pre<N>,
- Tag патча,
- список ключевых изменений,
- время snapshot’а,
- другие служебные поля.

Пользователь вставляет файл в архив без изменений.

<a name="5-2-manifest-txt"></a>
### 5.2. Генерация manifest.txt
Ассистент создает `manifest.txt`, содержащий SHA256-хеш каждого файла в переданном архиве.  
Пользователь вставляет документ в архив.

<a name="5-3"></a>
### 5.3. Игнорирование мусора
Ассистент обязан игнорировать следующие элементы:
- `__pycache__/`
- `*.pyc`
- `dev/**`
- `exports/**`
- временные runtime-файлы
- `state.jsonl`
- `backup.json`
- старые baseline
- `.idea/`, `.vscode/`
- временные .cmd/.ps1 вне структуры
- health-логи
- `*.tmp`
- временные txt/json

Эти файлы не участвуют в аудите.

<a name="5-4-readme-audit-md"></a>
### 5.4. Генерация README_AUDIT.md
Ассистент создает документ `README_AUDIT.md`, содержащий:
- активную baseline-версию,
- текущий STEP,
- список изменённых модулей,
- статус переноса UI,
- статус TPSL,
- что нельзя трогать,
- что нужно проверить,
- что удалить/исправить.

Пользователь копирует файл в архив.

<a name="5-5-partial-zip"></a>
### 5.5. Partial ZIP режим
Ассистент обязан формировать точный список файлов, необходимых для анализа.  
Пользователь отправляет только эти файлы.

Если в ходе патча требуется дополнительный файл, ассистент
запрашивает его отдельным partial ZIP.

---

Эти правила являются обязательными и обеспечивают ускоренный,
предсказуемый и стандартизированный процесс аудита.

<a name=""></a>
## Итог

Версия v1.1 объединяет все правила разработки MontrixBot и добавляет
механизм автоматического запроса файлов для патчей, делая процесс
стабильным, контролируемым и предсказуемым.

1.3.2 Runtime Init Rule: стартовая инициализация UI из persistent-состояния допускается только через UIAPI.get_runtime_state_snapshot() в одном helper-методе верхнего уровня (например, App._init_from_runtime_state). Контроллеры и виджеты не должны напрямую вызывать runtime_state или UIAPI.get_runtime_state_snapshot().

--------------------------------------------


<a name="5-anti-overheat-protocol-v1-0"></a>
## 5. Anti-Overheat Protocol v1.0

Дополнительно:
- соответствие Roadmap A/B обязательно проверяется до начала патча;


Для предотвращения перегрева модели и обеспечения стабильной работы патчей вводится следующий обязательный протокол.

<a name="5-1"></a>
### 5.1. Двухэтапная генерация патча
1. Перед любыми кодовыми изменениями ассистент генерирует **Patch Plan** — краткий, структурированный план.
2. Только после подтверждения пользователя генерируется **полный патч**.

<a name="5-2"></a>
### 5.2. Ограничение размера патча
- В один патч допускается касание **не более 2 файлов**, если заранее не оговорено иное.
- Объём diff должен быть минимальным и локализованным.

<a name="5-3"></a>
### 5.3. Разделение тяжёлых процессов
Ассистент разделяет:
- генерацию патча,
- генерацию audit/meta/manifest,
- генерацию тестовых чеклистов.

Это уменьшает нагрузку на reasoning и повышает стабильность ответа.

<a name="5-4-partial-zip"></a>
### 5.4. Partial ZIP как стандарт
Пользователь отправляет только те файлы, которые ассистент запросил.
Это снижает объём анализа и уменьшает когнитивную нагрузку.

<a name="5-5"></a>
### 5.5. Запрет на сцепленные большие операции
В одном патче запрещено совмещать:
- архитектурные изменения,
- правки UI event loop,
- обновления runtime,
- обновления TPSL,
- переносы модулей.

Каждый тип изменения выполняется отдельным патчем.

<a name="5-6"></a>
### 5.6. Ограничение размера ответа
Если размер ответа может превысить безопасный порог, ассистент обязан:
1. разбить результат на несколько сообщений;
2. использовать структурирование блоков;
3. избегать избыточных повторов.

<a name="5-7"></a>
### 5.7. Внутренние проверки перед генерацией патча
Ассистент выполняет:
- быструю проверку архитектурных связей,
- сверку с Master Rules,
- оценку сложности патча,
- проверку потенциальных конфликтов.

Если патч слишком сложный, ассистент должен предложить разбиение на подзадачи.

<a name="5-8"></a>
### 5.8. Непрерывная синхронизация памяти
Если чат был перезапущен или восстановлен — ассистент обязан загрузить:
- MasterRules,
- текущий baseline,
- Roadmap A/B,
- последние патчи.

Это предотвращает рассогласование контекстов.

<a name="5-9"></a>
### 5.9. Признаки перегрева (и действия)
Если модель может перегреться:
- слишком большой diff,
- слишком много файлов,
- глубокие пересечения слоёв,
- много проверок контекста,

ассистент обязан предупредить пользователя и предложить:
- уменьшить масштаб патча,
- разбить задачу,
- разделить генерацию на шаги.

<a name="5-10"></a>
### 5.10. Цель протокола
Гарантировать:
- стабильность,
- предсказуемость,
- отсутствие обрывов ответа,
- корректную и безопасную генерацию патчей.


<a name="6-update-policy"></a>

## 6. Update Policy

### 6.3. Ответственность за обновление
Ассистент обязан:
- после каждого патча проверять, затрагивает ли изменение архитектуру,
- при необходимости генерировать изменения для MasterRules,
- предоставлять пользователю готовый блок для вставки,
- контролировать совместимость с предыдущими версиями.


- Документ обновляется по мере изменения архитектуры.
- Версии фиксируются как MasterRules v1.x.
- Все обновления проходят через ассистента с проверкой совместимости.

<a name="7-patch-artifacts-linking"></a>
## 7. Patch Artifacts & Linking (Roadmap ↔ Rules ↔ Audit)

### 7.1. Иерархия артефактов

1. **MontrixBot_Roadmap_v2.0_Master** — источник истины по целям и STEP:
   - определяет активный STEP (например, 1.3.2 Runtime-Save),
   - описывает зависимости между шагами (1.3.3 зависит от 1.3.2 и т.д.),
   - задаёт статусы PLANNED / ACTIVE / DONE / ARCHIVED.

2. **MontrixBot_MasterRules** — источник истины по способу реализации:
   - содержит архитектурные HARD-правила (Code Rules),
   - описывает процессные стандарты (Development Standard),
   - включает Addendum, Extended Rules и Anti-Overheat Protocol.

3. **audit_bundle.json** — паспорт конкретного патча / baseline:
   - `baseline.name` — имя текущего baseline в формате Baseline Naming Rule,
   - `baseline.step` / `tag` / `pre` — точный STEP и параметры версии,
   - `baseline.previous` / `next` — связь с соседними baseline,
   - `roadmap.working` / `extended_ref` — связь с Roadmap,
   - `files.changed` — перечень файлов, изменённых в рамках данного патча,
   - `notes` — краткое описание сути изменений.

4. **manifest_min.txt** — минимальный манифест целостности:
   - содержит SHA256-хэш только для:
     - файлов из `files.changed` в `audit_bundle.json`,
     - обязательных служебных файлов (например, `VERSION`, `settings.json`),
   - используется для проверки корректности baseline и архива,
   - генерируется скриптом `tools/gen_manifest.py` в режиме `min`.

### 7.2. Связь Roadmap ↔ audit_bundle

1. `baseline.step` в `audit_bundle.json` **обязан** соответствовать активному STEP в рабочем Roadmap.
2. `baseline.tag` должен отражать смысл патча в рамках этого STEP (например, `RuntimeSave`).
3. При переходе к следующему STEP:
   - Roadmap обновляется (переводит текущий STEP в DONE / обновляет статусы),
   - создаётся новый `audit_bundle.json` с новым `baseline.name` и `pre<N>`,
   - MasterRules остаётся неизменным, если не требуется обновления правил.

### 7.3. Связь audit_bundle ↔ manifest_min

1. `audit_bundle.json` является единственным источником правды о том,
   **какие файлы входят в патч** (список `files.changed`).
2. `manifest_min.txt` **обязан** строиться только на основе:
   - `files.changed` из `audit_bundle.json`,
   - фиксированного набора ALWAYS_INCLUDE (например, `VERSION`, `settings.json`).
3. Любые другие файлы проекта не попадают в `manifest_min.txt` и игнорируются
   в рамках целостности данного патча.

### 7.4. Упразднённые постоянные артефакты

Следующие файлы **не являются частью репозитория** и могут генерироваться
ассистентом при необходимости, но не должны храниться постоянно в проекте:

- `project_meta.json`
- `manifest.txt` (полный)
- `README_AUDIT.md`
- промежуточные версии Roadmap (Extended/A/B вне Master-документа)

Их функция считается заменённой связкой:

- `MontrixBot_Roadmap_v2.0_Master_v*.md`,
- `MontrixBot_MasterRules_v*.md`,
- `audit_bundle.json`,
- `manifest_min.txt`.

### 7.5. Требования к патчу

Перед генерацией патча ассистент обязан:

1. Проверить актуальный STEP и статус в `MontrixBot_Roadmap_v2.0_Master`.
2. Свериться с `MontrixBot_MasterRules` (Code Rules, Development Standard,
   Addendum, Extended Rules, Anti-Overheat Protocol).
3. Сформировать или обновить `audit_bundle.json` для текущего baseline:
   - корректное имя baseline,
   - корректный STEP / Tag / pre,
   - актуальный список `files.changed`,
   - понятные `notes`.
4. Сгенерировать `manifest_min.txt` через `tools/gen_manifest.py` (режим `min`)
   на основе `audit_bundle.json`.
5. В явном виде указать пользователю:
   - новое имя baseline,
   - какие файлы изменены,
   - что и как протестировать,
   - какие артефакты (логи / скриншоты / runtime) собрать после тестирования.
