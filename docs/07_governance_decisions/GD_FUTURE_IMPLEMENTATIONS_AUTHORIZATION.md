# 📜 Governance Decision  
## GD_FUTURE_IMPLEMENTATIONS_AUTHORIZATION

**Статус:** APPROVED  
**Уровень:** Governance Decision  
**Дата:** 2025-01-XX  
**Применимость:** MontrixBot (все версии ≥ v2.2.4)  
**Связанные документы:**  
- MontrixBot_Governance.md  
- MontrixBot_MasterRules.md  
- MontrixBot_Product_Roadmap.md  
- GD_STEP_2_X_AUTO_REAL.md  

---

## 1. Назначение документа

Данный Governance Decision **явно разрешает к реализации** ряд функциональных направлений, ранее описанных в Roadmap и концептуальных документах, **но не реализованных** в STEP 1.x – STEP 2.2 включительно.

Документ:
- **снимает запрет на реализацию** перечисленных направлений,
- **НЕ активирует их автоматически**,
- **НЕ изменяет текущее поведение системы**,
- **НЕ отменяет STEP-дисциплину**.

Реализация любого из перечисленных пунктов возможна **только в рамках STEP** и в полном соответствии с Governance и MasterRules.

---

## 2. Область действия

Данное решение:
- **не отменяет** и **не изменяет** предыдущие Governance Decisions,
- **не модифицирует** STEP 1.x (который остаётся immutable),
- **не активирует Auto-REAL**,
- **не расширяет UI control-surface**,
- **не даёт права неконтролируемой реализации**.

---

## 3. Разрешённые направления (к реализации в будущих STEP)

Следующие направления **РАЗРЕШАЮТСЯ К РЕАЛИЗАЦИИ** при соблюдении STEP-дисциплины и всех инвариантов:

### 3.1 Торговые стратегии
- Реализация торговых стратегий **строго по каноническим Strategy Contracts**.
- Стратегии не могут нарушать инварианты STEP 1.x.
- Реализация стратегий **сама по себе не активирует торговлю**.

---

### 3.2 SIM-стратегии
- Разрешена реализация стратегий в **SIM-режиме**.
- SIM и REAL остаются **жёстко изолированными**.
- SIM-стратегии **не имеют влияния на REAL execution**.

---

### 3.3 Backtest / Replay / Incident tooling
- Backtest engine.
- Replay торговых сессий.
- Incident / post-mortem tooling.
- Инструменты диагностики и воспроизводимости.

Все перечисленное относится к **аналитическому и SIM-уровню** и не влияет на REAL execution.

---

### 3.4 Monitoring & Reporting (read-only)
- Портфельные и торговые отчёты.
- PnL, метрики, агрегаты.
- Экспорт данных (read-only).

Monitoring и Reporting не могут включать control-surface или управление системой.

---

### 3.5 UI-расширения (display-only)
- Визуализация статусов, gate verdicts, аналитики.
- Улучшение наблюдаемости и объяснимости.

Запрещено:
- добавление новых команд,
- добавление скрытых действий,
- расширение control-surface.

---

### 3.6 Risk / Policy Engine (dry / analytical)
- Risk и Policy логика **в аналитическом режиме**.
- Explainability, оценки, расчёты.

Запрещено:
- принятие решений,
- влияние на execution,
- автоматические действия.

---

### 3.7 GateEngine с влиянием на исполнение
- Разрешается **как направление**.
- Реализация возможна **только при открытии отдельного STEP**.
- Не активируется данным документом.

---

### 3.8 Multi-strategy / Portfolio orchestration
- Разрешается как направление.
- Реализация **только при отдельном STEP**.
- Не активируется автоматически.

---

### 3.9 Auto-pause / Auto-resume
- Разрешается как направление.
- Реализация **только при отдельном STEP**.
- Требует отдельной архитектурной фиксации.

---

### 3.10 Copy-trading
- **SIM copy-trading разрешён**.
- **REAL copy-trading запрещён** до отдельного Governance Decision.

---

## 4. О STEP-дисциплине и форме открытия STEP

Настоящий документ **НЕ является STEP** и **НЕ заменяет STEP**.

При этом допускается, что STEP может быть открыт **без создания нового STEP-документа**, если одновременно выполняются все условия:

- реализация явно ограничена рамками данного Governance Decision;
- границы STEP выражены **поведением системы и патчами**, а не текстовым документом;
- каждый патч имеет явный version label, указывающий STEP;
- запрещённые области (REAL execution, Auto-REAL, control-surface) не затрагиваются;
- любое расширение за пределы разрешённого требует отдельного Governance Decision или STEP-документа.

Факт начала реализации в разрешённых рамках **считается фактическим открытием STEP**.

---

## 5. Явные ограничения

Настоящий документ **НЕ разрешает**:

- Auto-REAL в любой форме.
- Автоматические действия без участия человека.
- GateEngine как финальный decision-maker.
- Прямое влияние стратегий на REAL execution без STEP.
- Расширение UI control-surface.
- Обход Trading Permission / Safety Wiring.

---

## 6. Итоговая фиксация

- Запрет на перечисленные направления **снят**.
- Реализация возможна **только по STEP-дисциплине**.
- Допускается открытие STEP **без отдельного STEP-документа**, при строгом соблюдении границ.
- Текущее состояние системы **не изменено**.
- STEP 1.x – STEP 2.2 считаются **CLOSED**.

---

**Конец документа**
