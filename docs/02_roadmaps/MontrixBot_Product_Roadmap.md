⚠️ ВАЖНО:
Наличие функциональности в Product Roadmap
НИКОГДА не является разрешением на реализацию.

Единственным источником допустимости
является Master Roadmap + Governance.

## Важное ограничение роли

Product Roadmap описывает планируемое содержание версий,
но не даёт разрешения на реализацию.

Каждый пункт данного документа:
- подчиняется Master Roadmap,
- проверяется через Governance,
- может быть отложен или отменён
без изменения данного файла.

Данный документ не даёт разрешения на реализацию.
Все пункты Product Roadmap подчиняются ограничениям
MontrixBot_Master_Roadmap и текущему ACTIVE STEP.

> Примечание: наличие функциональности в Product Roadmap
> не означает автоматической допустимости её реализации
> вне рамок активного STEP Master Roadmap.

# MontrixBot Product Roadmap
### Reality-Based, Code-Audited Development Plan (v1.1 → v3.x)

Этот roadmap основан **на фактическом аудите кода**, а не на абстрактных планах.  
Он отражает реальное состояние проекта и поэтапный путь от рабочего торгового движка
к полноценной торговой платформе и экосистеме.

Текущая точка запуска UI:
`python -m ui.main_app`

---

### Trading Strategies Delivery

Реализация торговых стратегий осуществляется
в соответствии с **MontrixBot_Master_Roadmap.md**.

Каждый этап (1.6.x, 1.7.x, …) добавляет
поддержку новых стратегий без изменения
ранее реализованных.

---

## Как читать этот roadmap

- **v1.1.1 – v1.4.x**  
  Функциональность, которая **уже реализована и работает** (возможны технические огрехи).

- **v1.5.x – v1.9.x**  
  Критический этап превращения движка в **полноценный продукт**, пригодный для широкой аудитории.

- **v2.x.x**  
  Масштабирование: портфели, стратегии, монетизация, профессиональные инструменты.

- **v3.x.x**  
  AI, арбитраж, экосистема, сообщество.

---

# 🟢 v1.1.x — Core SIM + UI Foundation

## v1.1.1 — SIM Engine Foundation — ✅ реализовано

**Смысл версии:** фундамент проекта. Рабочий торговый движок с UI.

**Что есть:**
- SIM-движок ордеров и исполнений — ✅ реализовано
- Buy / Close / Panic Sell — ✅ реализовано
- TP / SL (market + limit) — ✅ реализовано
- Step-trailing SL — ✅ реализовано
- Event bus и логирование — ✅ реализовано
- Проверки `minQty / minNotional` — ✅ реализовано
- Runtime-state + atomic save — ✅ реализовано
- Fake tick stream — ✅ реализовано
- Dark Theme — ✅ реализовано
- Desktop UI + headless режим — ✅ реализовано

Примечание: канонический headless entrypoint — `python -m scripts.run_headless --smoke` (UI не импортируется).

---

## v1.2.0 — TPSL Autoloop & Safety — ✅ реализовано

**Смысл версии:** автоматизация сопровождения позиции и базовая безопасность.

**Что есть:**
- TPSL bootstrap + autoloop — ✅ реализовано
- Dynamic SL — ✅ реализовано (volatility-aware trail step)
- SAFE MODE — ✅ реализовано (SAFE_HARD_LOCK в why_not)
- SIM ↔ REAL режимы — ✅ реализовано
- File-based safety unlock — ✅ реализовано
- Rate-limit + retry (exponential backoff) — ✅ реализовано
- Panic-stop логика — ✅ реализовано

Примечание: контракт v1.2.0 подтверждён тестом `python -m scripts.test_v1_2_0_contract`.


---

## v1.3.0 — Strategy & REAL Trading — ⚠️ частично реализовано

**Смысл версии:** переход от симуляции к реальной торговле.

### Что есть:

- Binance Spot API (REAL) — ✅ реализовано
- EMA / Volume фильтры — ✅ реализовано
- Advisor / Signals engine — ✅ реализовано
- Health-скрипты — ✅ реализовано
- Manual Confirm Surface (REAL execution, gated) — ✅ реализовано

### Autonomous Trading Mode (детерминированный):

- система сама принимает решение торговать или оставаться в бездействии — ✅ реализовано (intent loop)
- при неопределённости предпочитает бездействие — ✅ реализовано
- полностью подчиняется Safe Mode и ограничениям риска — ✅ реализовано


---

## v1.4.0 — Journal, Indicators, UI Maturity — ✅ реализовано **100%**

**Смысл версии:** появляется анализ и обратная связь по торговле.

**Что есть:**
- Trade Journal (таблица сделок) — ✅ реализовано  
- Экспорт CSV / JSON / XLSX — ✅ реализовано  
- График цены (line chart) — ✅ реализовано  
- RSI — ✅ реализовано  
- MACD — ✅ реализовано  
- Toast-уведомления в UI — ✅ реализовано  
- Snapshot → UI sync — ✅ реализовано  
- Runtime persistence — ✅ реализовано  

**Итог v1.4.x:**  
> Рабочий торговый движок с UI, ориентированный на опытного пользователя.

---

# 🟡 v1.5.x — Charts & UX Closure (КРИТИЧЕСКИЙ ЭТАП)

- Чёткое разделение аналитических (SIM)
  и исполнительных (REAL) индикаторов:
  REAL использует только минимально
  необходимые детерминированные условия исполнения для исполнения,
  без исследовательной аналитики.

- SIM Analytical Framework (канон):
  - документ MontrixBot_SIM_Analytical_Framework.md фиксирует допустимые индикаторы SIM,
    формат ScoutNotes/рекомендаций и запреты (не сигналы, не команды),
  - используется как Source of Truth для любых SIM-индикаторов в UI.


## v1.5.0 — Charts v2 
**Смысл версии:** переход от технического интерфейса к трейдерскому.

**Что реализовано:**
- Свечной график (OHLC) — ✅ реализовано
- Таймфреймы: Tick / 1m / 5m / 15m / 1h — ✅ реализовано
- EMA20 / EMA50 поверх цены — ✅ реализовано
- Линии Entry / TP / SL — ✅ реализовано (UI-only)
- Индикатор актуальности данных (snapshot age) — ✅ реализовано
- Визуальное объяснение причин SAFE MODE — ✅ реализовано

---

## v1.6.0 — UX & Usability 
**Смысл версии:** приложение становится удобным и безопасным.

**Что будет:**
- Вкладки / навигация — ✅ реализовано

---

---
## v1.8.0 — Portable & Replay 
- Replay tools (incident / readiness / replay) — ✅ реализовано

**Смысл версии:** автономность и удобство.

**Что будет:**
- Replay торговых сессий — ✅ реализовано
---

## v1.9.0 — Release-grade Stability — ⚠️ реализовано ~60%

- Audit & contracts (status / explain / ui) — ✅ реализовано
- SAFE MODE / guard rails — ✅ реализовано
- Policy trace & evidence — ✅ реализовано

**Итог v1.x:**  
> Полноценное desktop-приложение для SIM и REAL торговли.

---

# 🔵 v2.x.x — Scale & Monetization


## v2.3.0 — Monitoring & Reports

- Таблицы позиций / сделок (UI) — ✅ реализовано
- Explain-based диагностика состояния — ✅ реализовано
- PnL графики — ❌ не реализовано
- Portfolio Dashboard (единый экран) — ❌ не реализовано
- Расширенные XLSX отчёты — ⚠️ частично реализовано
- Google Sheets Sync — ❌ не реализовано
- Live Monitoring (read-only sync) — ✅ разрешено каноном
- Portfolio Dashboard — ❌ не реализовано
- PnL-графики — ❌ не реализовано
- XLSX отчёты — ⚠️ частично реализовано
- Google Sheets — ❌ не реализовано
- Live Sync — ❌ не реализовано
- Подсветка сделок на графике — ✅ реализовано
- График является главным элементом UI — ✅ реализовано
- Визуализация синхронизирована со state snapshots — ⚠️ частично реализовано
- SAFE MODE при устаревших данных — ⚠️ частично реализовано (детектор + overlay)
- Стратегия **«2 якоря + 1 ракета»** — ❌ не реализовано
- Turbo-режим (эксперимент) — ❌ не реализовано
- сама управляет входом/выходом и сопровождением — ✅ реализовано в SIM; ⚠️ intent-only в REAL
- работает без ручного подтверждения сделок — 🚫 запрещено каноном
- One-click SmartTrade (шаблоны сделки: план входа + выходы + риск-политика, профили Safe / Default / Pro) — ❌ не реализовано
- Подсказки TP / SL — ❌ не реализовано
- Двойное подтверждение REAL-режима — ❌ не реализовано
- Guard-rails: max-loss, max-order — ⚠️ частично реализовано
- Зафиксированные performance-цели:
  - Cold start < 3 секунд — ❌ не реализовано
  - RAM ≤ 200 MB — ❌ не реализовано
  - FPS ≥ 30 — ❌ не реализовано
- Упрощение control-surface:
  - SIM Start/Stop и REAL Start/Stop как единственные верхнеуровневые управляющие кнопки — ❌ не реализовано
  - удаление/скрытие режимного переключателя "Mode: SIM/REAL" из основного UI — ❌ не реализовано
  - "REAL CLI" и "Dry-Run" переводятся в DEV/Advanced (не основной UX) — ❌ не реализовано
- ScoutNotes & Recommendations (read-only) v1
  - отображение ScoutNotes/рекомендаций SIM в UI (без управляющих действий) — ⚠️ частично реализовано
  - ручная фильтрация по category/priority/confidence — ❌ не реализовано
  - отсутствие ScoutNotes является нормой — ✅ реализовано
- Telegram-уведомления 
  - входы — ❌ не реализовано
  - TP / SL — ❌ не реализовано
  - ошибки — ❌ не реализовано
  - PnL — ❌ не реализовано
- Фильтры и уровни уведомлений — ❌ не реализовано
- История уведомлений — ❌ не реализовано
- Звуки (опционально) — ❌ не реализовано
- System Notifications — ⚠️ частично реализовано
- Telegram-уведомления:
  - входы — ❌ не реализовано
  - TP / SL — ❌ не реализовано
  - ошибки — ❌ не реализовано
  - PnL — ❌ не реализовано
- Фильтры и уровни уведомлений — ❌ не реализовано
- История уведомлений — ❌ не реализовано
- Звуки (опционально) — ❌ не реализовано
- System Notifications
  - события SAFE MODE и деградаций — ⚠️ частично реализовано
  - блокировки политик и risk limits — ❌ не реализовано
  - готовность incident / replay данных — ❌ не реализовано
- ScoutNotes hygiene v1 (SIM)
  - TTL для ScoutNotes — ✅ реализовано
  - Anti-Spam / rate-limit — ⚠️ частично реализовано
  - dedupe однотипных сообщений — ✅ реализовано
  - уровни priority: INFO / WARNING / RISK (как метаданные, не команды) — ✅ реализовано
  - Portable binary (.exe) — ❌ не реализовано
- Backtest mode (product) — ❌ не реализовано
- Recovery wizard — ❌ не реализовано
- Portable `.exe` — ❌ не реализовано
- Offline mode (read-only) — ❌ не реализовано
- Мастер восстановления после краша — ❌ не реализовано
- Deterministic Replay with Assertions
  - воспроизведение с проверкой инвариантов и ожидаемых событий — ⚠️ частично реализовано
  - обнаружение расхождений поведения между версиями — ❌ не реализовано
- Incident Mode (Failure Replay)
  - режим расследования: воспроизведение контекста до/после сбоя — ✅ реализовано
  - фиксация «момента отказа» + диагностический пакет состояния — ⚠️ частично реализовано
- Backtest Mode
  - пользовательский режим поверх replay и assertions — ❌ не реализовано
  - запуск и анализ исторических торговых сессий — ❌ не реализовано
  - без влияния на REAL и runtime state — ❌ не реализовано
- SIM Self-Quality Metrics (internal)
  - метрики качества ScoutNotes: доля устаревших (TTL), дубли, распределение priority — ❌ не реализовано
  - используется для самодиагностики SIM и улучшения качества сообщений — ❌ не реализовано
  - не является фидбеком по торговым решениям REAL — ✅ реализовано
- Formal trade lifecycle states — ❌ не реализовано
- Long soak tests — ❌ не реализовано
- Soak-тест ≥ 1 часа — ❌ не реализовано
- Корректная обработка гэпов TP / SL — ❌ не реализовано
- Версионирование состояния и миграции — ⚠️ частично реализовано
- State Schema Versioning
  - формализация схемы state (обязательные/устаревшие поля, совместимость) — ⚠️ частично реализовано
  - валидация state по версии схемы (совместимо с migrations и invariant checks) — ❌ не реализовано
- User-guide (PDF / MD) — ⚠️ частично реализовано
- Усиленный SAFE MODE — ⚠️ частично реализовано
- Policy Engine (ограничения по риску и деградации) — ⚠️ частично реализовано
- Защита от устаревших данных и задержек рыночных фидов — ⚠️ частично реализовано
- Мастер восстановления после сбоев — ❌ не реализовано
- Явная модель жизненного цикла сделки (Trade Lifecycle States)
  - формализованные состояния позиции (created / open / closing / closed / aborted) — ❌ не реализовано
  - единый контракт для journal, replay и policy engine — ❌ не реализовано
- Decision Reason Codes
  - фиксация причин входа, отказа и выхода — ⚠️ частично реализовано
  - детерминированный decision trace для автономных решений — ❌ не реализовано
- Cold Start Diagnostic
  - отчёт загрузки состояния — ⚠️ частично реализовано
  - причины fallback и recovery — ⚠️ частично реализовано
  - активные ограничения policy engine — ❌ не реализовано
- Градации деградации системы
  - data degraded → read-only — ❌ не реализовано
  - executor degraded → SIM only — ❌ не реализовано
  - UI degraded → headless continue — ❌ не реализовано
- Invariant / Contract Validation Engine
  - периодическая проверка инвариантов состояния (SSOT, SIM/REAL изоляция, целостность lifecycle) — ❌ не реализовано
  - при нарушениях: фиксация причины + переход в деградацию / SAFE MODE — ❌ не реализовано
- Policy Decision Priority Resolver
  - детерминированный порядок приоритета политик (risk / perf / degradation / safety) — ❌ не реализовано
  - при конфликте решений — явный winner + reason code — ❌ не реализовано
- System Capability Matrix
  - явное описание текущих разрешённых возможностей системы (trade / observe / sim-only / read-only) — ❌ не реализовано
  - отображается в UI и используется Policy Engine — ❌ не реализовано
- Time & Clock Consistency Layer
  - единый контракт времени: event_time / snapshot_time / decision_time / execution_time — ⚠️ частично реализовано
  - учёт дрейфа и задержек (clock drift) как часть policy и stale guards — ❌ не реализовано
- Policy Evaluation Trace
  - трассировка применения политик (risk / data / perf / degradation) — ⚠️ частично реализовано
  - фиксация chain-of-decision поверх reason codes — ⚠️ частично реализовано
- Live Consistency Checker
  - runtime-детектор рассинхронов снапшотов, «залипших» состояний и silent failures — ❌ не реализовано
  - интеграция с деградацией и диагностикой (без зависимости UI от файлов) — ❌ не реализовано
- Graceful Shutdown Protocol
  - формализованная остановка: что сохраняется, что блокируется, что завершает executor — ❌ не реализовано
  - совместимо с persistence, safe mode и cold start diagnostic — ❌ не реализовано
- Trade Arming Gate
  - двухфазная модель исполнения: configure → arm → execute — ❌ не реализовано
  - блокировка любых REAL-действий до явного перевода системы в ARMED — ❌ не реализовано
  - интеграция с Capability Matrix и Safe Mode — ❌ не реализовано
- Exchange Connection Wizard
  - пошаговое подключение к бирже — ❌ не реализовано
  - проверка прав API-ключей и ограничений — ❌ не реализовано
  - напоминания: IP allowlist, резервные копии, синхронизация времени — ❌ не реализовано
- Scout Notes & Recommendations (SIM → REAL)
  - формирование аналитических наблюдений и рекомендаций в SIM — ⚠️ частично реализовано
  - передача в REAL в виде ScoutNotes (без команд и execution-инструкций) — ⚠️ частично реализовано
  - рекомендации не являются торговыми сигналами и могут быть проигнорированы REAL — ✅ реализовано
  - ScoutNote Contract (timestamp/scope/category/observation/context/recommendation?/confidence/priority/ttl) — ⚠️ частично реализовано
  - Anti-Spam, TTL и dedupe — ✅ реализовано
  - строго без зависимости торговой логики REAL от SIM — ✅ реализовано
- Free / Pro / Enterprise — ❌ не реализовано
- License Manager v1 — ❌ не реализовано
- Шифрование ключей — ❌ не реализовано
- Multi-account — ❌ не реализовано
- Разделение портфелей — ❌ не реализовано
- Configuration Versioning & Audit Trail
  - версионирование и история изменений конфигурации (risk limits, policy thresholds, strategy params) — ❌ не реализовано
  - привязка изменений к времени и режиму (SIM/REAL) — ❌ не реализовано
- Одновременный запуск нескольких стратегий — ❌ не реализовано
- UI-конструктор стратегий — ❌ не реализовано
- Доли депозита — ❌ не реализовано
- Auto-rotation — ❌ не реализовано
- Ручные детерминированные условия исполнения — ⚠️ частично реализовано
- Fault Domain Isolation
  - изоляция доменов отказов: data / execution / strategy / UI / external API — ❌ не реализовано
  - локальная деградация без остановки всей платформы — ❌ не реализовано
- Operator / Automation Boundary
  - явная граница ручных вмешательств и автономных действий — ⚠️ частично реализовано
  - audit trail для override / manual actions — ❌ не реализовано
- Strategy Capability Declaration
  - декларация возможностей/требований стратегии (REAL/SIM, latency sensitivity, partial fills, ограничения) — ❌ не реализовано
  - используется policy engine и portfolio orchestration — ❌ не реализовано
- Remote Command Interface
  - удалённый доступ к состоянию и диагностике — ❌ не реализовано
  - read-only режим по умолчанию — ❌ не реализовано
  - write-команды только через Operator Boundary и audit trail — ❌ не реализовано
- Trio / Top-K / Full-Auto — ❌ не реализовано
- Auto-balancing по результатам — ❌ не реализовано
- Copy-trading (SIM) — ❌ не реализовано
- Explainable Risk Decisions
  - объяснимые решения риск-политики (почему стратегия/портфель включён или выключен) — ❌ не реализовано
  - reason codes на уровне риска и аллокации — ⚠️ частично реализовано
- Risk Budget Model
  - формализованный «остаточный риск» и его распределение между стратегиями/портфелем — ❌ не реализовано
  - связь с explainable risk decisions и policy engine — ❌ не реализовано
- Formal trade lifecycle states — ❌ не реализовано
- Long soak tests — ❌ не реализовано
- Soak-тест ≥ 1 часа — ❌ не реализовано
- Корректная обработка гэпов TP / SL — ❌ не реализовано
- Версионирование состояния и миграции — ⚠️ частично реализовано
- State Schema Versioning
  - формализация схемы state (обязательные/устаревшие поля, совместимость) — ⚠️ частично реализовано
  - валидация state по версии схемы (совместимо с migrations и invariant checks) — ❌ не реализовано
- User-guide (PDF / MD) — ⚠️ частично реализовано
- Усиленный SAFE MODE — ⚠️ частично реализовано
- Policy Engine (ограничения по риску и деградации) — ⚠️ частично реализовано
- Защита от устаревших данных и задержек рыночных фидов — ⚠️ частично реализовано
- Мастер восстановления после сбоев — ❌ не реализовано
- Явная модель жизненного цикла сделки (Trade Lifecycle States)
  - формализованные состояния позиции (created / open / closing / closed / aborted) — ❌ не реализовано
  - единый контракт для journal, replay и policy engine — ❌ не реализовано
- Decision Reason Codes
  - фиксация причин входа, отказа и выхода — ⚠️ частично реализовано
  - детерминированный decision trace для автономных решений — ❌ не реализовано
- Cold Start Diagnostic
  - отчёт загрузки состояния — ⚠️ частично реализовано
  - причины fallback и recovery — ⚠️ частично реализовано
  - активные ограничения policy engine — ❌ не реализовано
- Градации деградации системы
  - data degraded → read-only — ❌ не реализовано
  - executor degraded → SIM only — ❌ не реализовано
  - UI degraded → headless continue — ❌ не реализовано
- Invariant / Contract Validation Engine
  - периодическая проверка инвариантов состояния (SSOT, SIM/REAL изоляция, целостность lifecycle) — ❌ не реализовано
  - при нарушениях: фиксация причины + переход в деградацию / SAFE MODE — ❌ не реализовано
- Policy Decision Priority Resolver
  - детерминированный порядок приоритета политик (risk / perf / degradation / safety) — ❌ не реализовано
  - при конфликте решений — явный winner + reason code — ❌ не реализовано
- System Capability Matrix
  - явное описание текущих разрешённых возможностей системы (trade / observe / sim-only / read-only) — ❌ не реализовано
  - отображается в UI и используется Policy Engine — ❌ не реализовано
- Time & Clock Consistency Layer
  - единый контракт времени: event_time / snapshot_time / decision_time / execution_time — ⚠️ частично реализовано
  - учёт дрейфа и задержек (clock drift) как часть policy и stale guards — ❌ не реализовано
- Policy Evaluation Trace
  - трассировка применения политик (risk / data / perf / degradation) — ⚠️ частично реализовано
  - фиксация chain-of-decision поверх reason codes — ⚠️ частично реализовано
- Live Consistency Checker
  - runtime-детектор рассинхронов снапшотов, «залипших» состояний и silent failures — ❌ не реализовано
  - интеграция с деградацией и диагностикой (без зависимости UI от файлов) — ❌ не реализовано
- Graceful Shutdown Protocol
  - формализованная остановка: что сохраняется, что блокируется, что завершает executor — ❌ не реализовано
  - совместимо с persistence, safe mode и cold start diagnostic — ❌ не реализовано
- Trade Arming Gate
  - двухфазная модель исполнения: configure → arm → execute — ❌ не реализовано
  - блокировка любых REAL-действий до явного перевода системы в ARMED — ❌ не реализовано
  - интеграция с Capability Matrix и Safe Mode — ❌ не реализовано
- Exchange Connection Wizard
  - пошаговое подключение к бирже — ❌ не реализовано
  - проверка прав API-ключей и ограничений — ❌ не реализовано
  - напоминания: IP allowlist, резервные копии, синхронизация времени — ❌ не реализовано
- Scout Notes & Recommendations (SIM → REAL)
  - формирование аналитических наблюдений и рекомендаций в SIM — ⚠️ частично реализовано
  - передача в REAL в виде ScoutNotes (без команд и execution-инструкций) — ⚠️ частично реализовано
  - рекомендации не являются торговыми сигналами и могут быть проигнорированы REAL — ✅ реализовано
  - ScoutNote Contract (timestamp/scope/category/observation/context/recommendation?/confidence/priority/ttl) — ⚠️ частично реализовано
  - Anti-Spam, TTL и dedupe — ✅ реализовано
  - строго без зависимости торговой логики REAL от SIM — ✅ реализовано
- Free / Pro / Enterprise — ❌ не реализовано
- License Manager v1 — ❌ не реализовано
- Шифрование ключей — ❌ не реализовано
- Multi-account — ❌ не реализовано
- Разделение портфелей — ❌ не реализовано
- Configuration Versioning & Audit Trail
  - версионирование и история изменений конфигурации (risk limits, policy thresholds, strategy params) — ❌ не реализовано
  - привязка изменений к времени и режиму (SIM/REAL) — ❌ не реализовано
- Одновременный запуск нескольких стратегий — ❌ не реализовано
- UI-конструктор стратегий — ❌ не реализовано
- Доли депозита — ❌ не реализовано
- Auto-rotation — ❌ не реализовано
- Ручные детерминированные условия исполнения — ⚠️ частично реализовано
- Fault Domain Isolation
  - изоляция доменов отказов: data / execution / strategy / UI / external API — ❌ не реализовано
  - локальная деградация без остановки всей платформы — ❌ не реализовано
- Operator / Automation Boundary
  - явная граница ручных вмешательств и автономных действий — ⚠️ частично реализовано
  - audit trail для override / manual actions — ❌ не реализовано
- Strategy Capability Declaration
  - декларация возможностей/требований стратегии (REAL/SIM, latency sensitivity, partial fills, ограничения) — ❌ не реализовано
  - используется policy engine и portfolio orchestration — ❌ не реализовано
- Remote Command Interface
  - удалённый доступ к состоянию и диагностике — ❌ не реализовано
  - read-only режим по умолчанию — ❌ не реализовано
  - write-команды только через Operator Boundary и audit trail — ❌ не реализовано
- Trio / Top-K / Full-Auto — ❌ не реализовано
- Auto-balancing по результатам — ❌ не реализовано
- Copy-trading (SIM) — ❌ не реализовано
- Explainable Risk Decisions
  - объяснимые решения риск-политики (почему стратегия/портфель включён или выключен) — ❌ не реализовано
  - reason codes на уровне риска и аллокации — ⚠️ частично реализовано
- Risk Budget Model
  - формализованный «остаточный риск» и его распределение между стратегиями/портфелем — ❌ не реализовано
  - связь с explainable risk decisions и policy engine — ❌ не реализовано

---

## v2.4.0 — Pro Tools — ⚠️ реализовано ~10%

- Custom Indicators (Python) — ❌ не реализовано
- TA-Lib — ❌ не реализовано
- Backtest engine — ❌ не реализовано
- История портфеля — ❌ не реализовано
- PDF / Excel отчёты — ⚠️ частично реализовано
- Pump v1 — ❌ не реализовано
- Simulation Fidelity Levels — ❌ не реализовано
  - уровни точности SIM (simplified / realistic / stress) — ❌ не реализовано
  - управление источниками данных и моделью исполнения без смешения с REAL — ❌ не реализовано

---

## v2.5.0 — Multi-exchange Framework — ❌ не реализовано

- KuCoin — ❌ не реализовано
- Kraken — ❌ не реализовано
- Gemini — ❌ не реализовано
- Bybit — ❌ не реализовано
- OKX — ❌ не реализовано
- Copy-trading (REAL) — ❌ не реализовано

---

# 🧠 v3.x.x — AI, Arbitrage, Ecosystem

## v3.0.0 — AI Signals (beta) — ⚠️ реализовано ~10%
- News / Social analysis — ⚠️ частично реализовано
- AI Signals tab — ❌ не реализовано
- Telegram `/ai` — ❌ не реализовано
- Bandits (ε-greedy / UCB / Thompson Sampling) — ❌ не реализовано
- Auto-switch портфеля — ❌ не реализовано
- Self-learning — ❌ не реализовано
- Deterministic Randomness — ❌ не реализовано
  - фиксируемые random seed для воспроизводимости решений и экспериментов — ❌ не реализовано
  - совместимо с replay и incident mode — ❌ не реализовано

---

## v3.1.0 — Pump v2 (AI) — ⚠️ реализовано ~10%
- LSTM / Transformer — ❌ не реализовано
- AI стратегии — ❌ не реализовано
- Risk management — ⚠️ частично реализовано
- AI vs Classic — ❌ не реализовано
- Auto-tuning параметров — ❌ не реализовано

---

## v3.2.0 — Arbitrage — ❌ не реализовано

- Меж- и внутрибиржевой арбитраж — ❌ не реализовано
- Учёт комиссий и ликвидности — ❌ не реализовано
- Auto-limits — ❌ не реализовано
- Отчёты — ❌ не реализовано

---

## v3.3.0 — Ecosystem — ❌ не реализовано

- Профиль трейдера — ❌ не реализовано
- Cloud backup & restore — ❌ не реализовано
- Marketplace стратегий — ❌ не реализовано
- Marketplace плагинов — ❌ не реализовано
- Cloud License v2 — ❌ не реализовано

---

## v3.4.0 — Community & Builder — ❌ не реализовано

- Форум / чат — ❌ не реализовано
- Рейтинги и отзывы — ❌ не реализовано
- Mobile app (beta) — ❌ не реализовано
- Strategy Builder (drag & drop) — ❌ не реализовано
- Copy / Share стратегий — ❌ не реализовано

### SIM Delivery (Canonical)

| Версия   | SIM / аналитика |
| ------- | ---------------- |
| 1.5.x   | SIM Analytical Framework + базовый набор индикаторов SIM |
| 1.6.0   | ScoutNotes/Recommendations v1 (read-only UI) |
| 1.7.0   | TTL / Anti-Spam / Dedupe + priority metadata |
| 1.8.0   | SIM Self-Quality Metrics (internal) |
| 1.9.0   | ScoutNote Contract + релизная формализация канала SIM→REAL |