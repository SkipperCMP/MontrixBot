"""
PanicController (STUB)

Назначение:
UI-контроллер для обработки кнопки PANIC и экстренных действий пользователя.

Важно:
– бизнес-логика паники и SafeMode находится в core
– данный контроллер только маршрутизирует пользовательское действие

На текущем этапе:
– прямое управление не реализовано
– файл существует как архитектурная точка входа
"""

class PanicController:
    def __init__(self, ui_api=None):
        self.ui_api = ui_api

    def trigger(self):
        """
        Пользователь инициировал PANIC.

        Реальная логика будет:
        – через ui_api
        – с переводом системы в SafeMode
        """
        return
